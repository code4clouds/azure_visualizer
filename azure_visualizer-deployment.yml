apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: azure-visualizer
spec:
  replicas: 1
  template:
    metadata:
      labels:
        k8s-app: azure-visualizer
    spec:
      containers:
      - name: azure-visualizer
        image: casatix.azurecr.io/dcasati/azure_visualizer
        imagePullPolicy: "IfNotPresent"
        ports:
        - containerPort: 5000
